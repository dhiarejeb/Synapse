/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NoteResponseDto } from '../../models/note-response-dto';

export interface GetNoteById$Params {
  boardId: string;
  noteId: string;
}

export function getNoteById(http: HttpClient, rootUrl: string, params: GetNoteById$Params, context?: HttpContext): Observable<StrictHttpResponse<NoteResponseDto>> {
  const rb = new RequestBuilder(rootUrl, getNoteById.PATH, 'get');
  if (params) {
    rb.path('boardId', params.boardId, {});
    rb.path('noteId', params.noteId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NoteResponseDto>;
    })
  );
}

getNoteById.PATH = '/api/v1/boards/{boardId}/notes/{noteId}';
